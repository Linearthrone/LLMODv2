<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Windows 11 Glass Effect Brush -->
    <SolidColorBrush x:Key="GlassBackground" Color="#B3000000"/>
    <SolidColorBrush x:Key="GlassBorder" Color="#44FFFFFF"/>
    <SolidColorBrush x:Key="GlassForeground" Color="#FFFFFFFF"/>
    <SolidColorBrush x:Key="GlassSecondary" Color="#BBFFFFFF"/>
    
    <!-- Windows 11 Accent Colors -->
    <SolidColorBrush x:Key="Win11Accent" Color="#FF0078D4"/>
    <SolidColorBrush x:Key="Win11AccentLight" Color="#FF40E0D0"/>
    <SolidColorBrush x:Key="Win11Surface" Color="#F3F3F3"/>
    <SolidColorBrush x:Key="Win11OnSurface" Color="#323130"/>
    
    <!-- Button Styles -->
    <Style x:Key="GlassButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource GlassBackground}"/>
        <Setter Property="Foreground" Value="{StaticResource GlassForeground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource GlassBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="FontFamily" Value="Segoe UI Variable"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="RootBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding CornerRadius}">
                        
                        <!-- Glass Effect Background -->
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#E6000000" Offset="0"/>
                                <GradientStop Color="#E6111111" Offset="1"/>
                                <GradientStop Color="#22FFFFFF" Offset="0.5"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        
                        <!-- Backdrop Effect (WinUI 3) -->
                        <Border.Background>
                            <ImageBrush ImageSource="ms-appx:///Assets/glass-texture.png" Opacity="0.1"/>
                        </Border.Background>
                        
                        <ContentPresenter x:Name="ContentPresenter"
                                          Content="{TemplateBinding Content}"
                                          Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Foreground="{TemplateBinding Foreground}"/>
                        
                        <!-- Visual States -->
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal"/>
                                
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Target="RootBorder.Background" Value="#E61A1A1A"/>
                                        <Setter Target="RootBorder.BorderBrush" Value="#44FFFFFF"/>
                                    </VisualState.Setters>
                                </VisualState>
                                
                                <VisualState x:Name="Pressed">
                                    <VisualState.Setters>
                                        <Setter Target="RootBorder.Background" Value="#E60F0F0F"/>
                                        <Setter Target="RootBorder.BorderBrush" Value="#66FFFFFF"/>
                                    </VisualState.Setters>
                                </VisualState>
                                
                                <VisualState x:Name="Disabled">
                                    <VisualState.Setters>
                                        <Setter Target="RootBorder.Background" Value="#66666666"/>
                                        <Setter Target="RootBorder.BorderBrush" Value="#22FFFFFF"/>
                                        <Setter Target="ContentPresenter.Foreground" Value="#66FFFFFF"/>
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- TextBox Style -->
    <Style x:Key="GlassTextBox" TargetType="TextBox">
        <Setter Property="Background" Value="#33000000"/>
        <Setter Property="Foreground" Value="{StaticResource GlassForeground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource GlassBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,10"/>
        <Setter Property="FontFamily" Value="Segoe UI Variable"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="RootBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{TemplateBinding CornerRadius}">
                        
                        <!-- Glass Effect Background -->
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#44000000" Offset="0"/>
                                <GradientStop Color="#22000000" Offset="1"/>
                                <GradientStop Color="#11FFFFFF" Offset="0.5"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        
                        <ScrollViewer x:Name="ContentElement"
                                      Margin="{TemplateBinding Padding}"
                                      HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                                      VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
                                      Foreground="{TemplateBinding Foreground}"
                                      FontFamily="{TemplateBinding FontFamily}"
                                      FontSize="{TemplateBinding FontSize}"/>
                        
                        <!-- Visual States -->
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal"/>
                                
                                <VisualState x:Name="PointerOver">
                                    <VisualState.Setters>
                                        <Setter Target="RootBorder.BorderBrush" Value="#44FFFFFF"/>
                                        <Setter Target="RootBorder.Background" Value="#44000000"/>
                                    </VisualState.Setters>
                                </VisualState>
                                
                                <VisualState x:Name="Focused">
                                    <VisualState.Setters>
                                        <Setter Target="RootBorder.BorderBrush" Value="#66FFFFFF"/>
                                        <Setter Target="RootBorder.Background" Value="#55000000"/>
                                    </VisualState.Setters>
                                </VisualState>
                                
                                <VisualState x:Name="Disabled">
                                    <VisualState.Setters>
                                        <Setter Target="RootBorder.Background" Value="#22666666"/>
                                        <Setter Target="RootBorder.BorderBrush" Value="#11FFFFFF"/>
                                        <Setter Target="ContentElement.Foreground" Value="#66FFFFFF"/>
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ComboBox Style -->
    <Style x:Key="GlassComboBox" TargetType="ComboBox">
        <Setter Property="Background" Value="#33000000"/>
        <Setter Property="Foreground" Value="{StaticResource GlassForeground}"/>
        <Setter Property="BorderBrush" Value="{StaticResource GlassBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="FontFamily" Value="Segoe UI Variable"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="CornerRadius" Value="8"/>
    </Style>

    <!-- ScrollViewer Style -->
    <Style x:Key="GlassScrollViewer" TargetType="ScrollViewer">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
    </Style>

    <!-- Chat Message Bubble Style -->
    <Style x:Key="UserMessageBubble" TargetType="Border">
        <Setter Property="Background" Value="#E61E40AF"/>
        <Setter Property="BorderBrush" Value="#33FFFFFF"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="12,12,4,12"/>
        <Setter Property="Padding" Value="16,12"/>
        <Setter Property="Margin" Value="40,8,8,8"/>
    </Style>

    <Style x:Key="AssistantMessageBubble" TargetType="Border">
        <Setter Property="Background" Value="#E6252525"/>
        <Setter Property="BorderBrush" Value="#22FFFFFF"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="12,12,12,4"/>
        <Setter Property="Padding" Value="16,12"/>
        <Setter Property="Margin" Value="8,8,40,8"/>
    </Style>

    <Style x:Key="SystemMessageBubble" TargetType="Border">
        <Setter Property="Background" Value="#44444444"/>
        <Setter Property="BorderBrush" Value="#22FFFFFF"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="Margin" Value="20,4"/>
    </Style>

    <!-- Animation Resources -->
    <Storyboard x:Key="FadeInAnimation">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="0" To="1" Duration="0:0:0.3">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut"/>
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

    <Storyboard x:Key="SlideInFromRight">
        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                         From="50" To="0" Duration="0:0:0.4">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut"/>
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

    <Storyboard x:Key="PulseAnimation">
        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         From="0.7" To="1" Duration="0:0:1"
                         AutoReverse="True" RepeatBehavior="Forever"/>
    </Storyboard>

    <!-- Default Styles Application -->
    <Style TargetType="Button" BasedOn="{StaticResource GlassButton}"/>
    <Style TargetType="TextBox" BasedOn="{StaticResource GlassTextBox}"/>
    <Style TargetType="ComboBox" BasedOn="{StaticResource GlassComboBox}"/>
    <Style TargetType="ScrollViewer" BasedOn="{StaticResource GlassScrollViewer}"/>

</ResourceDictionary>