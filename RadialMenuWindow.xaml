<Window x:Class="LLMOverlay.RadialMenuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RadialMenu" 
        Height="300" Width="250"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False">
    
    <Window.Resources>
        <!-- Rounded Button Template -->
        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="25">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid>
        <!-- Background with Glass Effect -->
        <Grid.Background>
            <RadialGradientBrush>
                <GradientStop Color="#E6000000" Offset="0"/>
                <GradientStop Color="#E6111111" Offset="0.7"/>
                <GradientStop Color="#22FFFFFF" Offset="1"/>
            </RadialGradientBrush>
        </Grid.Background>
        
        <Border BorderBrush="#44FFFFFF" BorderThickness="1" CornerRadius="20">
            <Border BorderBrush="#22FFFFFF" BorderThickness="1" CornerRadius="19">
                <Grid Margin="10">
                    <!-- Central Button -->
                    <Button x:Name="CenterButton"
                            Width="50" Height="50"
                            Background="#40000000"
                            BorderBrush="#66FFFFFF"
                            BorderThickness="1"
                            Style="{StaticResource RoundedButton}"
                            Click="CenterButton_Click">
                        <TextBlock Text="❌" FontSize="20" Foreground="White" 
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>
                    
                    <!-- Radial Buttons positioned in a circle -->
                    <Button x:Name="LastContact1Button"
                            Width="45" Height="45"
                            Background="#40FF6B6B"
                            BorderBrush="#66FFFFFF"
                            BorderThickness="1"
                            Margin="0,-100,0,0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Top"
                            Style="{StaticResource RoundedButton}"
                            Click="LastContact1Button_Click">
                        <TextBlock Text="👤" FontSize="18" Foreground="White" 
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>
                    
                    <Button x:Name="LastContact2Button"
                            Width="45" Height="45"
                            Background="#404ECDC4"
                            BorderBrush="#66FFFFFF"
                            BorderThickness="1"
                            Margin="0,-70,70,35"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top"
                            Style="{StaticResource RoundedButton}"
                            Click="LastContact2Button_Click">
                        <TextBlock Text="👥" FontSize="18" Foreground="White" 
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>
                    
                    <Button x:Name="ContactDirectoryButton"
                            Width="45" Height="45"
                            Background="#409B59B6"
                            BorderBrush="#66FFFFFF"
                            BorderThickness="1"
                            Margin="0,0,100,0"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Style="{StaticResource RoundedButton}"
                            Click="ContactDirectoryButton_Click">
                        <TextBlock Text="📇" FontSize="18" Foreground="White" 
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>
                    
                    <Button x:Name="LoadModelButton"
                            Width="45" Height="45"
                            Background="#40FFA07A"
                            BorderBrush="#66FFFFFF"
                            BorderThickness="1"
                            Margin="0,70,70,0"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Bottom"
                            Style="{StaticResource RoundedButton}"
                            Click="LoadModelButton_Click">
                        <TextBlock Text="🤖" FontSize="18" Foreground="White" 
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>
                    
                    <Button x:Name="CreateContactButton"
                            Width="45" Height="45"
                            Background="#40FFD700"
                            BorderBrush="#66FFFFFF"
                            BorderThickness="1"
                            Margin="0,100,0,0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Bottom"
                            Style="{StaticResource RoundedButton}"
                            Click="CreateContactButton_Click">
                        <TextBlock Text="➕" FontSize="18" Foreground="White" 
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>
                </Grid>
            </Border>
        </Border>
    </Grid>
</Window>